:8080 {
    encode zstd gzip

    # Route backend endpoints and docs to FastAPI
    @backend path /api/* /docs* /openapi.json /redoc* /static/*
    handle @backend {
        reverse_proxy localhost:8003
    }

    # Route everything else to Next.js frontend
    handle {
        reverse_proxy localhost:3000
    }
}
